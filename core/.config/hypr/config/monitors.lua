-- Universal Monitor Configuration (Safe Fallback)
-- Profile-specific monitor configurations override or augment this file via config.profile.monitors

-- Generic fallback for any connected monitor
hl.monitor({
    output    = "",
    mode      = "preferred",
    position  = "auto",
    scale     = 1,
})
