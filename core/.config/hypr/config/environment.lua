-- Environmental variables (Universal Core)
-- Profile-specific GPU drivers and hardware acceleration flags are loaded via config.profile.environment

hl.env("EDITOR", "nvim")
hl.env("VISUAL", "nvim")
