-- Desktop Mouse & Keyboard Input Configuration
hl.config({
    input = {
        -- Flat profile for precision desktop mouse tracking
        accel_profile = "flat",
        sensitivity = 0.0,
        kb_options = "",
        repeat_rate = 50,
        repeat_delay = 300,
    },
    cursor = {
        no_warps = false,
        inactive_timeout = 5,
    },
})
